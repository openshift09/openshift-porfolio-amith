“I created two namespaces: frontend and backend.
Then I applied a deny-all policy on backend so no one can access it.
After that, I created another policy that allows only the frontend namespace to access backend.
This shows secure microservice communication.”