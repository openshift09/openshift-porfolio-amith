# Project 5 — OpenShift ImageStream + BuildConfig (Beginner Friendly)

## 📌 Overview
This project shows how OpenShift automatically builds and updates images using:
- ImageStreams
- BuildConfig
- Deployment auto-update

---

## 🧠 Concept (Simple Explanation)

ImageStream = Notice board showing available image versions  
BuildConfig = Machine that builds new images  
Deployment = Application that runs the image  

When BuildConfig builds a new image → ImageStream updates → Deployment uses latest version.

---

## 📂 Files
